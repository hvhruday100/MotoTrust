# Database Package

Prisma schema, generated client, migrations, and seed logic.

PostgreSQL in Neon is the source of truth. Firebase is used for identity and possibly media storage, but business state belongs here.

