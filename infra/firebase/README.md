# Firebase

Firebase is used for authentication and can store proof media during the MVP.

Recommended setup:

1. Enable Firebase Authentication.
2. Start with phone/email sign-in.
3. Create Admin SDK service account credentials for the API.
4. If using Firebase Storage for proof media, restrict direct customer access and serve signed access through the API.

