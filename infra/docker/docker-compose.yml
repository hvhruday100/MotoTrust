services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mototrust
      POSTGRES_PASSWORD: mototrust
      POSTGRES_DB: mototrust
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres-data:

